    <div class="header">
    		<h1 class="title"><% if user_signed_in? %> Welcome, <%= current_user.username %>  <% else %> <% end %></h1>
    		  <%= render :partial =>"home/header" %>
    </div><!-- .header -->
<div class="section"> 
  <div class="article profile">
  	<h2 class="headshots">Edit Favorite movie of all time </h2><br>

    <% form_for [:user, @favmovie],:html => { :class=>'form', :id=>'add_photo' } do |f| %>
      
      <%- @favmovie.errors.full_messages.each do |message| -%>
        <div class="errorExplanation"> <p><%= message %></p></div>
      <%- end -%>
 
      <p class="text_box"> <%= f.label :title %></p>
      <p class="text_box1"> <%= f.text_area :title %></p>
      <div class="clearfloat"></div>
      
       
        <%= select(:favmovie, :smoke, [['----'], ['Yes'], ['Sometimes'], ['When drinking'], ['Trying to quit']],{:include_blank => 'Select'}) %>
        <div class="clearfloat"></div>
        
        <%= select(:favmovie, :drink, [['Very Often'], ['Often'], ['Socially'], ['Rarely'], ['Desperately'], ['Not at All']],{:include_blank => 'Select'}) %>
        <div class="clearfloat"></div>
      <p class="button" style="margin:10px 0 0 188px;"> <button type="submit"> Save </button>
	      <button type="submit" onclick="javascript:history.back()" > Cancel </button> 
	    </p>

    <% end %>

    </div><!-- article -->
    <!-- END Aritcle -->
</div><!-- .section -->